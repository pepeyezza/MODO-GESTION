# Copiar este archivo a ".env" para desarrollo local (nunca subir ".env" a git).
# En Vercel, DATABASE_URL la crea sola la integración de "Storage" > Postgres.

DATABASE_URL="postgresql://gestiona:localdev@localhost:5432/gestiona?schema=public"

# Generar uno propio, largo y al azar, por ejemplo con: openssl rand -hex 32
JWT_SECRET="reemplazar-por-un-texto-largo-y-al-azar"
